/*
Write an async function named: multiply

Requirements:
Accept two numbers.
Return their multiplication.
Do not use await.
Call the function.
Print the returned value.

*/

async function multiply(num1, num2) {
  return num1 * num2;
}

console.log(multiply(5, 10));

//ouput: Promise { 50 }
